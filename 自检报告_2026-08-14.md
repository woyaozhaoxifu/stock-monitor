# stock-monitor 全量自检报告
> 日期：2026-08-14 · 当前提交 `b7a6e51` · 工作区干净（本次仅验证，无代码改动）

## 总评
**数据 / 内容 / 功能 三方面全部健康，0 报错。** 后端数据结构自检 12/12 通过；真实板块数据联网抽查正常；前端全功能真机自检（Playwright 覆盖全部 /api 接口）`SELFCHECK_PASS`，console/page 报错为 0。

---

## 一、数据接口自检（后端 Python）
编译 `py_compile app.py` 通过；用桩函数替换网络逐一验证各数据函数返回结构：

| 函数 | 结果 | 关键数据 |
|---|---|---|
| `build_state()` | ✅ | ts/version/market/watchlist/global/summary 齐全 |
| `get_market_anomaly()` | ✅ | trends=120、events=5、**movers=5**（半导体↑/煤炭↓/CPO↑/创新药↑/电力↓）—— 异动标注数据源正常 |
| `get_rank()` | ✅ | 涨幅榜/跌幅榜各有数据 |
| `get_breadth()` | ✅ | 涨跌家数 up/down/flat |
| `search_stock()` | ✅ | 模糊搜索返回个股 |
| `get_sector_info()` | ✅ | 板块→成分股列表正常 |
| `get_stock_boards()` | ✅ | 个股→所属板块正常 |
| `get_stock_info()` | ✅ | 个股资料/F10 字段齐全 |
| `get_news()` | ✅ | 返回 `{src, items, updated, error}` 结构 |
| `get_billboard()` | ✅ | 龙虎榜条目正常 |
| `fetch_kline()`/`fetch_trends()` | ✅ | K线/分时字段完整 |
| 路由表完整性 | ✅ | 22 条 /api 路由均在 |

**真实联网抽查**：`fetch_boards()` 返回 **199 个具体行业/概念板块**（电子化学品Ⅱ/稀土永磁/通信设备…影视院线/电力/传媒），指数近似名 0 个，`source: em` —— 此前「板块非具体板块」修复持续生效。

---

## 二、内容展示自检（前端）
真机加载后各面板内容均正确渲染：

| 面板 | 渲染 |
|---|---|
| 大盘指数卡片 | 3 个（上证/深证/创业）✅ |
| 板块涨跌榜 | 6 个**具体行业**（半导体/煤炭/CPO/创新药/电力/通信设备）✅ |
| 自选股 | 2 个 ✅ |
| 个股涨跌榜 | 涨幅/跌幅各 2 ✅ |
| 龙虎榜 | 1 条 ✅ |
| 资讯 | 2 条 ✅ |
| 持仓 | 2 行 ✅ |
| 全球 | 2 个 ✅ |
| **大盘异动分时图** | 价格线 + **markLine=5 / markPoint=5**（板块异动向外连线 + 板块名+↑↓ 标注）✅ |

---

## 三、功能交互自检（前端真机）
| 功能 | 结果 |
|---|---|
| 分时图画线 | ✅ |
| 日K 画线 | ✅ |
| 周K 画线 | ✅ |
| 月K 画线 | ✅ |
| 画线颜色自选（🎨取色器） | 选 `#ff2200` 后系列色同步 + localStorage 持久化 ✅ |
| 点击板块 → 成分股弹窗 | 弹出 3 只成分股 ✅ |
| 点击个股卡片 → 资料弹窗 | 弹出「浦发银行」资料 ✅ |
| 模块拖拽排序 | 顺序变更 + `stockboard_module_order` 持久化 ✅ |
| **console / page 报错** | **0** ✅ |

---

## 待办
本地累计 11 个 commit 待 push：`99e3105 / a172293 / 93eb3a1 / 9c0321a / 2404ed9 / b9f7c10 / caf2d18 / 25fa999 / a6630c9 / 938764c / b7a6e51`。
**等 GitHub token → `git push origin main` + Render 重部署。**
